'use client';

// Replaced: the standalone images have been removed; the 3-image grid is now
// injected INSIDE each Experience card on the homepage.
export default function LiveImagesStrip() {
  return null;
}
